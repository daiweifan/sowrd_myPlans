package ${bean.packageUrl}.dao;

import org.springframework.data.repository.CrudRepository;

import ${bean.packageUrl}.model.${bean.name};



/** 
 * ${bean.content}的dao
 * @author ${bean.authorName}
 *
 * ${bean.date}
 */
public interface ${bean.name}Dao extends CrudRepository<${bean.name}, Long> {
	
	
}
